{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug My ESLint Rule",
      "program": "${workspaceFolder}/node_modules/eslint/bin/eslint.js",
      "args": [
        "${workspaceFolder}/src/test/index.ts" // 你的测试文件路径
        // "--rule",
        // "your-rule-name:off" // 可选：指定规则开关，调试时可调整
      ]
    }
  ]
}
